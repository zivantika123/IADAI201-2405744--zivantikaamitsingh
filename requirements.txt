streamlit==1.22.0
Pillow==9.5.0
numpy==1.23.5
scikit-learn==1.2.2
# Use the CPU build of TensorFlow for Streamlit sharing environments (smaller, more likely to install cleanly)
tensorflow-cpu==2.9.1
# Required to load .h5 Keras model files
h5py==3.1.0
# Note: do NOT list standard library modules like `shutil` here
