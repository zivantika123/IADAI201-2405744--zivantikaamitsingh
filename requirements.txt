# Core dependencies
streamlit==1.22.0
pillow==9.5.0

# Scientific computing - use versions with prebuilt wheels
numpy==1.23.5  # Stable version with prebuilt wheels
scikit-learn==1.2.2  # Compatible with numpy 1.23.5

# Deep Learning - CPU versions for better compatibility
tensorflow-cpu==2.9.1  # CPU version for better deployment
h5py==3.8.0  # Newer version with prebuilt wheels for Python 3.10

# Add protobuf explicitly to avoid conflicts
protobuf==3.19.6
